# CLAUDE.md

### Available Skills


- `/kahu` - Query Okahu SRE Agent API. Supports `--app <app_name>` with GitHub run IDs to fetch traces/spans and produce reports

# Project Notes

## Environment

- `.env` is in the repo root. Read it freely for API keys and config. Never modify it.

## Git

- Do not add Co-Authored-By lines to git commits.
